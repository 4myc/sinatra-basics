require 'sinatra' #load the Sinatra library

require_relative './app.rb' #requires our application file, defined in app.rb

run Application #uses run to start the application represented by the ruby class Application, which is defined in app.rb